{
  "name": "playground",
  "version": "2.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "parcel src/index.html",
    "build": "rimraf dist && parcel build src/index.html --public-url /react-jsonschema-form/",
    "gh-pages": "npm run build && gh-pages --dist dist/",
    "icons": "svgr --out-dir src/components/icons svg --icon"
  },
  "dependencies": {
    "@react-schema-form/bootstrap": "^2.0.0",
    "@react-schema-form/core": "^2.0.0",
    "atob": "^2.1.2",
    "codemirror": "^5.30.0",
    "react": ">=15",
    "react-codemirror2": "^5.0.4",
    "react-dom": "^16.4.1"
  },
  "devDependencies": {
    "@svgr/cli": "^2.0.0",
    "gh-pages": "^1.2.0",
    "parcel-bundler": "^1.9.2",
    "rimraf": "^2.6.2"
  },
  "prettier": {
    "singleQuote": true
  }
}
